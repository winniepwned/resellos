# Compliance-Check

$ARGUMENTS enthält: Scope ("all", "gdpr", "iso", "bsi")

Delegiere an **compliance-checker**:
- 64 Regeln total (16 DSGVO + 27 ISO + 21 BSI)
- Manifest-Validierung
- Frontend DSGVO-UI Prüfung
- Unified Report generieren

Bei FAIL: Priorisierte Fix-Liste → Implementiere Fixes → Re-Run
